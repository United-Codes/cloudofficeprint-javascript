const STATIC_OPTS = {
    tool: 'javascript',
    javascript_sdk_version: '21.1',
};

export class PrintJob {
    // template;
    // data;
    // server;
    // output_config;
    // subtemplates;
    // prepend_files;
    // append_files;

    // constructor(template, data, server, output_config, subtemplates, prepend_files, append_files) {
    //     this.template = template;
    //     this.data = data;
    //     this.server = server;
    //     this.output_config = output_config;
    //     this.subtemplates = subtemplates;
    //     this.prepend_files = prepend_files;
    //     this.append_files = append_files;
    // }
}
